# Web framework
Flask==2.3.2

# For secure password hashing and salting
passlib[bcrypt]==1.7.4

# For input validation and regex filtering
validators==0.20.0

# For securing HTTP headers
Flask-Talisman==1.0.0

# For form validation & CSRF protection
Flask-WTF==1.2.1

# For running with HTTPS locally (dev only)
pyOpenSSL==23.2.0

# For handling user sessions securely
Flask-Login==0.6.3

# For loading environment variables (for secrets)
python-dotenv==1.0.1

# (Optional) ORM + DB abstraction
SQLAlchemy==2.0.29
Flask-SQLAlchemy==3.1.1

# For linting and security auditing (optional dev tools)
bandit==1.7.7
